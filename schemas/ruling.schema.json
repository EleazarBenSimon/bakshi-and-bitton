{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/EleazarBenSimon/bakshi-and-bitton/schemas/ruling.schema.json",
  "title": "Bakshi&Bitton — Ruling",
  "description": "Structured factual record of a single Israeli Supreme Court ruling within the project's scope (see METHODOLOGY.md §1).",
  "type": "object",
  "required": [
    "case_id",
    "case_id_slug",
    "case_name_he",
    "case_name_en",
    "ruling_date",
    "panel",
    "petitioner_type",
    "respondent",
    "outcome",
    "official_url",
    "summary_he",
    "summary_en"
  ],
  "additionalProperties": false,
  "properties": {
    "case_id": {
      "type": "string",
      "description": "Official Hebrew case identifier as printed in the ruling, e.g. 'בג\"ץ 5658/23'. Use the exact string the court uses."
    },
    "case_id_slug": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "URL-safe filename slug, e.g. '5658-23'. Must match the filename (without .json)."
    },
    "case_name_he": {
      "type": "string",
      "description": "Hebrew case style: petitioner v. respondent."
    },
    "case_name_en": {
      "type": "string",
      "description": "English case style: petitioner v. respondent."
    },
    "ruling_date": {
      "type": "string",
      "format": "date",
      "description": "Date the final judgment was issued, ISO 8601."
    },
    "filing_date": {
      "type": ["string", "null"],
      "format": "date",
      "description": "Date the lead petition was filed. Null if not findable."
    },
    "panel": {
      "type": "array",
      "minItems": 1,
      "description": "Justices on the panel that decided this ruling.",
      "items": {
        "type": "object",
        "required": ["name_he", "name_en"],
        "additionalProperties": false,
        "properties": {
          "name_he": { "type": "string", "description": "Justice's name in Hebrew." },
          "name_en": { "type": "string", "description": "Justice's name in English." },
          "role": {
            "type": "string",
            "enum": ["President", "Acting President", "Deputy President", "Justice", "Retired Justice"],
            "description": "Court role at the time of this ruling. Use 'Retired Justice' for participation within the statutory post-retirement window."
          },
          "slug": {
            "type": "string",
            "pattern": "^[a-z0-9-]+$",
            "description": "Stable identifier for joining with data/justices/<slug>.json."
          }
        }
      }
    },
    "petitioner_type": {
      "type": "string",
      "enum": ["NGO", "Individual", "State", "MK", "Party", "Local-Authority", "Other"],
      "description": "Category of the lead petitioner."
    },
    "petitioner_name_he": {
      "type": ["string", "null"],
      "description": "Hebrew name of the lead petitioner (NGO, individual, party, etc.). Use 'בעילום שם' for anonymous if applicable."
    },
    "petitioner_name_en": {
      "type": ["string", "null"],
      "description": "English transliteration or translation of the lead petitioner's name."
    },
    "respondent": {
      "type": "string",
      "enum": ["Government", "Prime Minister", "Minister", "Cabinet", "Senior-Appointments-Committee", "Knesset", "Local-Authority", "Statute", "Other"],
      "description": "Identity of the primary respondent whose decision is challenged. Use 'Statute' when the challenge is to primary legislation itself (e.g. via private commercial litigation that becomes a constitutional vehicle) rather than to an executive decision."
    },
    "respondent_decision_he": {
      "type": ["string", "null"],
      "description": "Hebrew description of the specific government decision being challenged."
    },
    "respondent_decision_en": {
      "type": ["string", "null"],
      "description": "English description of the specific government decision being challenged."
    },
    "doctrine_invoked": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "reasonableness",
          "proportionality",
          "basic_law_human_dignity_and_liberty",
          "basic_law_freedom_of_occupation",
          "basic_law_judiciary",
          "basic_law_government",
          "conflict_of_interest",
          "procedural_review",
          "ultra_vires",
          "natural_justice",
          "constituent_authority_limits",
          "abuse_of_constituent_power",
          "constitutional_supremacy",
          "separation_of_powers",
          "judicial_independence",
          "other"
        ]
      },
      "description": "Doctrines explicitly named by the ruling. Do not infer doctrines the ruling did not name. See METHODOLOGY §2."
    },
    "outcome": {
      "type": "string",
      "enum": ["struck_down", "partially_struck", "remanded", "restricted", "upheld", "dismissed", "modified", "declarative", "mandatory_order", "warning_of_voidness"],
      "description": "Disposition of the petition. 'upheld' = petition granted; 'dismissed' = denied; 'declarative' = petition formally dismissed/upheld but the ruling's main contribution is a doctrinal pronouncement (Mizrahi 1995, Dakka 1976); 'mandatory_order' = court compelled the respondent to act (typical for ministerial-inaction petitions); 'warning_of_voidness' = התראת בטלות, where the Court declines to strike the challenged measure but signals that future similar measures will be void under the doctrinal test articulated (Shafir 2021 is the canonical example)."
    },
    "vote_majority": {
      "type": ["integer", "null"],
      "minimum": 1,
      "description": "Number of justices in the majority/winning position. Null if not findable or if the ruling was unanimous (use vote_majority equal to panel size)."
    },
    "vote_minority": {
      "type": ["integer", "null"],
      "minimum": 0,
      "description": "Number of justices in dissent or alternative position. Zero for unanimous."
    },
    "majority_authors": {
      "type": "array",
      "items": { "type": "string", "pattern": "^[a-z0-9-]+$" },
      "description": "Slugs of justices who authored opinions in the majority. Empty if no separate opinions identified."
    },
    "minority_authors": {
      "type": "array",
      "items": { "type": "string", "pattern": "^[a-z0-9-]+$" },
      "description": "Slugs of justices who authored dissenting opinions."
    },
    "official_url": {
      "type": "string",
      "format": "uri",
      "description": "URL of the official ruling text. Strongly prefer supremedecisions.court.gov.il. See METHODOLOGY §5."
    },
    "secondary_urls": {
      "type": "array",
      "items": { "type": "string", "format": "uri" },
      "description": "Optional: Versa/Cardozo translations, IDI articles, authoritative archives. Do not include news commentary."
    },
    "summary_he": {
      "type": "string",
      "minLength": 20,
      "maxLength": 600,
      "description": "Factual one-to-two-sentence Hebrew summary. See METHODOLOGY §4 — no editorializing."
    },
    "summary_en": {
      "type": "string",
      "minLength": 20,
      "maxLength": 600,
      "description": "Factual one-to-two-sentence English summary. See METHODOLOGY §4."
    },
    "predicate_ag_opinion": {
      "type": ["string", "null"],
      "description": "If the cabinet/government acted against a prior Attorney General opinion, brief reference to that opinion (date + topic). Null otherwise."
    },
    "respondent_body": {
      "type": ["string", "null"],
      "description": "Specific statutory or executive body whose action is being reviewed (e.g. 'Senior Appointments Advisory Committee', 'Gronis Committee'). Distinct from broader respondent category."
    },
    "compliance_state": {
      "type": "string",
      "enum": ["pending", "complied", "contested", "n/a"],
      "description": "Has the government complied with the ruling? 'pending' = compliance in progress; 'complied' = effective; 'contested' = ruling stands but defiance signals exist; 'n/a' = no compliance question (petition dismissed)."
    },
    "defiance_signals": {
      "type": ["string", "null"],
      "description": "Free-text note on any government refusal to comply, public statements of intent to defy, or legislative work-arounds. When this field contains specific claims about named individuals, defiance_signals_sources MUST be populated. Editorial language (qualifiers like 'controversial', 'shocking', 'expansive') is grounds for PR rejection — see METHODOLOGY §4."
    },
    "defiance_signals_sources": {
      "type": "array",
      "items": { "type": "string", "format": "uri" },
      "description": "URLs supporting specific claims in defiance_signals. Required when defiance_signals references specific public statements, named officials, or specific legislative actions. May reference URLs already in secondary_urls."
    },
    "tags": {
      "type": "array",
      "items": { "type": "string", "pattern": "^[a-z0-9-]+$" },
      "description": "Optional editorial tags for cross-cutting filtering. Examples: 'appointment-block', 'security-sector', 'religion-state', 'post-oct7'."
    },
    "target_branch": {
      "type": ["string", "null"],
      "enum": ["executive", "legislature", "basic_law", "mixed", null],
      "description": "Power-transfer index axis (see METHODOLOGY-power-transfer.md §3.1). The operative target of the petition: 'executive' = ministerial/government/cabinet/AG decision; 'legislature' = primary Knesset legislation; 'basic_law' = a Basic Law amendment itself; 'mixed' = substantive challenge to more than one branch. Null permitted for legacy records pending classification."
    },
    "domain": {
      "type": ["string", "null"],
      "enum": [
        "religion-state",
        "asylum-immigration",
        "settlements-and-land",
        "senior-executive-appointments",
        "cabinet-composition",
        "civil-tort-and-state-liability",
        "privatization-of-sovereign-functions",
        "conscription-and-equality",
        "economic-regulation",
        "intelligence-and-security-appointments",
        "prison-conditions",
        "criminal-procedure",
        "judicial-appointments",
        "basic-law-amendment",
        "administrative-review-general",
        "local-government",
        null
      ],
      "description": "Power-transfer index axis (see METHODOLOGY-power-transfer.md §3.2). Closed vocabulary; additions require a methodology version bump. Null permitted for legacy records pending classification."
    },
    "competence_class": {
      "type": ["string", "null"],
      "enum": ["acquisition", "extension", "application", "ratification", null],
      "description": "Power-transfer index classification (see METHODOLOGY-power-transfer.md §4). 'acquisition' = first ruling in this (target_branch × domain) cell (weight +3); 'extension' = meaningful sub-domain or doctrinal frontier within an already-claimed cell (weight +1); 'application' = routine application of settled doctrine (weight 0); 'ratification' = petition dismissed, no new doctrine (weight 0). Null permitted for legacy records pending classification. 'acquisition' codings require three independent secondary sources cited in notes."
    },
    "adversarial_view": {
      "type": ["string", "null"],
      "description": "Optional contributor-cited counter-classification or counter-interpretation. Used when a reasonable, source-supported alternative reading exists for this ruling's competence_class, doctrine_invoked, or material framing. Must include source URLs. See METHODOLOGY-power-transfer.md §8."
    },
    "notes": {
      "type": ["string", "null"],
      "description": "Free-text notes on disputed facts, source conflicts, or contextual information that doesn't belong in summary fields. See METHODOLOGY §7."
    }
  }
}
