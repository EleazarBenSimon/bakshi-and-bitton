# Dependencies
node_modules/
package-lock.json

# Build output
_site/
.cache/
dist/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*~

# Local config
.env
.env.local

# AI coding-assistant artifacts — NEVER publish (Rule #1 / Rule #2)
# This is a public repo; these must stay local-only.
.claude/
CLAUDE.md
.clauderc
AGENTS.md
GEMINI.md
.cursor/
.cursorrules
.aider*
.continue/
.mcp.json
.github/copilot-instructions.md

# Logs
*.log
npm-debug.log*

# Local build & cutover backups — public repo, keep local-only (Rule #1 / Rule #2)
.build/
.cutover-backup-*/
.pytest_cache/

# Comic MANUFACTURE / production drafts — NEVER publish (Rule #1 / Rule #2).
# Only FINISHED, hand-selected comic pages (site/comic-<id>.html + their final
# panels under site/assets/comics/<id>/) belong on the public repo. The scripts,
# storyboards, asset deltas, and raw pre-selection panels stay local-only — they
# also embed absolute /Users paths that must never reach the alias repo.
scripts/comic-storyboards/
site/assets/comics/662-11/
