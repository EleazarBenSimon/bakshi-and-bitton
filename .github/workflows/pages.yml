name: Deploy site to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'site/**'
      - 'content/**'
      - 'data/**'
      - 'scripts/build.py'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      # Pillow + python-bidi are required by the ruling-day kit step,
      # which renders the per-ruling share cards used as each page's
      # og:image. Without them build.py exits 2 before writing the
      # sitemap, robots.txt and llms.txt, and the deploy fails.
      - name: Install build dependencies
        run: pip install markdown Pillow python-bidi

      - name: Build site data (rulings, justices, content)
        run: python3 scripts/build.py

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload site artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site/

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
